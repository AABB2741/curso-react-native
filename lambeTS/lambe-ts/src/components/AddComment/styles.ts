import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        paddingHorizontal: 20,
        flexDirection: "row",
        alignItems: "center",
        marginTop: 10
    },
    caption: {
        marginLeft: 10,
        fontSize: 12,
        color: "#ccc"
    },
    input: {
        flex: 1
    }
});
